# URL de base de l'API (par défaut : /api/v1, proxifié par Vite en dev et nginx en prod)
VITE_API_URL=/api/v1

# Cible du proxy Vite en dev (backend FastAPI local)
VITE_PROXY_TARGET=http://localhost:8000
