# ── Serveur de PRODUCTION ─────────────────────────────────────────────────────
# Déployé par le workflow GitHub Actions `deploy-production.yml` (images GHCR :latest / :vX.Y.Z).
# TLS automatique via Caddy (Let's Encrypt) — définir DOMAIN dans .env.prod.
# Usage : docker compose -f docker-compose.prod.yml --env-file .env.prod up -d
services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - pgdata_prod:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 10

  backend:
    image: ${REGISTRY:-ghcr.io}/${IMAGE_NAMESPACE}/a-la-source-backend:${IMAGE_TAG:-latest}
    restart: unless-stopped
    # Migrations + création du compte admin (idempotent), puis serveur.
    command: sh -c "alembic upgrade head && python -m app.initial_data && uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 2"
    environment:
      ENVIRONMENT: production
      DEBUG: "false"
      DATABASE_URL: postgresql+psycopg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      SECRET_KEY: ${SECRET_KEY}
      CORS_ORIGINS: ${CORS_ORIGINS}
      FRONTEND_URL: ${FRONTEND_URL}
      EMAIL_BACKEND: ${EMAIL_BACKEND:-smtp}
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASSWORD: ${SMTP_PASSWORD}
      EMAIL_FROM: ${EMAIL_FROM}
      NOTIFY_INTERNAL_EMAIL: ${NOTIFY_INTERNAL_EMAIL}
      CATALOGUE_PDF_PATH: /app/data/catalogue.pdf
      # Compte admin initial (créé au 1er démarrage) ; pas de données de démo en prod.
      FIRST_ADMIN_EMAIL: ${FIRST_ADMIN_EMAIL}
      FIRST_ADMIN_PASSWORD: ${FIRST_ADMIN_PASSWORD}
      SEED_DEMO_DATA: ${SEED_DEMO_DATA:-0}
    volumes:
      - catalogue_data:/app/data
    depends_on:
      db:
        condition: service_healthy

  frontend:
    image: ${REGISTRY:-ghcr.io}/${IMAGE_NAMESPACE}/a-la-source-frontend:${IMAGE_TAG:-latest}
    restart: unless-stopped
    depends_on:
      - backend

  # Reverse proxy TLS (Let's Encrypt automatique) — SEC-01 : HTTPS partout
  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      DOMAIN: ${DOMAIN}
      # Sous-domaine du staging (servi via le port 8080 de l'hôte) ; valeur neutre tant qu'il n'est pas défini.
      STAGING_DOMAIN: ${STAGING_DOMAIN:-staging.localhost}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - frontend

volumes:
  pgdata_prod:
  catalogue_data:
  caddy_data:
  caddy_config:
